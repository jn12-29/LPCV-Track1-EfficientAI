aimet-onnx==2.26.0; sys_platform == 'linux' and python_version == "3.10"
transformers==4.45.0
sentencepiece==0.2.1
psutil; sys_platform != 'win32'
wmi; sys_platform == 'win32'
onnx==1.18.0
# Must be CPU torch so ONNX export produces consistent op names across environments.
torch==2.10.0
torchvision==0.25.0
onnxruntime==1.22
